/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['i.pravatar.cc'], 
  },
};

module.exports = nextConfig;

module.exports = {
  images: {
      domains: ['your-domain.com'],
      unoptimized: true, 
  },
};
